vhost:
  image: simpledrupalcloud/vhost:latest
  hostname: vhost
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - vhostdata
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  environment:
    - SERVER_NAME=localhost
vhostdata:
  image: simpledrupalcloud/data:latest
  hostname: vhostdata
  volumes:
    - /vhost
