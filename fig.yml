vhost:
  image: simpledrupalcloud/vhost:dev
  hostname: vhost
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  environment:
    - SERVER_NAME=localhost
